/**
 * ME, DIGITALLY — the future home of an AI version of Arusha.
 *
 * The route (/me), the "?" in the navigation and the page already exist.
 * To bring it to life: set `enabled: true` and implement `askClone` in
 * ./client.js against a real endpoint. The UI doesn't need to change.
 */
export const cloneConfig = {
  enabled: false,
  name: 'ME, DIGITALLY',
  endpoint: null,
}
